Words = input('Введите слова: ')
def split(W):
    Res = W.split()
    return Res

def leng(W):
    Res = len(W)
    return W

Result = split(Words)
print(Result)

Leng = leng(Result)
print("Слов в списке:" , len(Leng))

Q = input("Формируем второй список с количеством символов, равному количеству введенных слов?: ")
if Q == "да" or Q == "Да" or Q == "ДА":
    print('Введите ',len(Result),' символа(-ов): ')
    Symbols = input()
    Result2 = split(Symbols)
    Leng2 = leng(Result2) 
    if (len(Leng)) != (len(Leng)):
        print('Вы ввели недопустимое количество символов.')
        exit()
    else: dictionary = dict(zip(Result, Result2))
    print(dictionary)

elif Q == "Нет" or Q == "нет" or Q == "НЕТ":
    print("Второй список не был сформирован.")
else:print("Неверный ввод.")
print("Программа завершила свою работу.")
